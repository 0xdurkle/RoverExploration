{
  "name": "rover-dashboard-api",
  "version": "1.0.0",
  "description": "API server for Rover Exploration dashboard",
  "main": "dist/index.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "if [ -f tsconfig.json ]; then tsc; else cd dashboard-api && tsc; fi",
    "start": "if [ -f dist/index.js ]; then node dist/index.js; else cd dashboard-api && node dist/index.js; fi",
    "dev": "ts-node src/index.ts",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "pg": "^8.11.3",
    "discord.js": "^14.14.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "@types/cors": "^2.8.17",
    "@types/pg": "^8.10.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
